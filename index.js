// index.js
import { AppRegistry } from 'react-native';
import App from './App.js'; // make sure this path points to your App component

// Register the app component
AppRegistry.registerComponent('main', () => App);